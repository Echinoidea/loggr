use crate::app::App;
use ratatui::layout::*;
use ratatui::style::*;
use ratatui::text::*;
use ratatui::widgets;
use ratatui::widgets::*;
use ratatui::Frame;

pub fn ui(frame: &mut Frame, app: &App) {
    let chunks = Layout::default().direction();
}
